# Application
NODE_ENV=development
PORT=8080

# Database — non-sensitive (ConfigMap in K8s)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=groove
DB_USER=postgres

# Database — sensitive (Secret / Secret Manager in GCP)
DB_PASSWORD=changeme-local-only
